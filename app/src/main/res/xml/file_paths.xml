<?xml version="1.0" encoding="utf-8"?>
<!-- Only the exports folder inside the cache is shareable. Nothing else in the app's
     storage is reachable through the provider. -->
<paths>
    <cache-path name="exports" path="exports/" />
</paths>
